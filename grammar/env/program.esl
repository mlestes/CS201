
VAR i = 30; //global varible: program : expression

STRUCT test //struct definintion: program : structdef
BEGIN
     VAR attr1; //variable declaration vardef : VAR VARIABLE SEMI_COLON
     VAR attr2;
     IF(i < 30)
     BEGIN
         VAR attr3;
     END
     ELSE
     BEGIN
         VAR attr4;
     END
END

DEFINE FuncDef(var1 var2 var3) //function definition: program : funcdef
BEGIN
    testStruct = newStruct(var1 var2 var3);
    VAR x = 1;
    VAR y = -1;
    VAR z;
    VAR a;
    DEFINE nestFun(h l j k) //nested function: statement : funcdef
    BEGIN
        RETURN ((h ^ l) % j) / k;
    END
    z = x * y;
    //next is a ternary: statement : ternary SEMI_COLON
    //ternary : expression QUESTION expression COLON expression
    a = z >= testStruct.attr2 ? "This is false" : "This is the string";
    WHILE(x < 10)
    BEGIN
       z = var1 + var2 * (var3 * x);
       x++;
       IF(var3 * x > 20)
       BEGIN
           BREAK;
       END
    END
    RETURN a;
END

DEFINE TestFun()
BEGIN
    VAR a = 1;
    VAR b = 2;
    VAR c = 3 % b - a;
    FuncDef(a b c);
    IF(c == 0 AND i !== 15)
    BEGIN
        PRINT(FuncDef(c b a));
    END
    PRINT("i is now equal to " i " and c is " c);
END
